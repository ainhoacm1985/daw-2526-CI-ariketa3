{
    "name": "ainhoacm1985/php-ci-demo",
    "description": "Demo PHP con CI en GitHub Actions",
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "scripts": {
        "test": "phpunit --colors=always --testdox"
    }
}
